# Forge runtime state (per-developer)
.forge/state.md
.forge/state.json
.forge/progress/
.forge/worktrees/
.forge/.forge-loop.lock
.forge/token-ledger.json
.forge/resume.md
.forge/handoff/
.forge/synthesis-log.jsonl
.forge/checkpoints/
.forge/history/

# Runtime data — never commit dumps, materials, or synthesis output
# Matches the runtime vault wherever it lives (repo root or app/),
# while leaving app/src/vault/ source code tracked because that path
# isn't named just "vault"
/vault/
/app/vault/

# Node
node_modules/
dist/
.vite/
*.log
*.tsbuildinfo

# OS
.DS_Store
Thumbs.db
